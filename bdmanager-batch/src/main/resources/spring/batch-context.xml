<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:batch="http://www.springframework.org/schema/batch"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-2.1.xsd">

	<!-- INFRASTRUCTURE -->
	<bean id="jobRepository" class="org.springframework.batch.core.repository.support.MapJobRepositoryFactoryBean">
		<property name="transactionManager" ref="transactionManager" />
	</bean>

	<bean id="transactionManager" class="org.springframework.batch.support.transaction.ResourcelessTransactionManager" />

	<bean id="jobLauncher" class="org.springframework.batch.core.launch.support.SimpleJobLauncher">
		<property name="jobRepository" ref="jobRepository" />
	</bean>

	<!-- JOB -->
	<batch:job id="helloWorldJob" job-repository="jobRepository">
		<batch:step id="hello">
			<batch:tasklet ref="helloWolrdTasklet" />
		</batch:step>
	</batch:job>
	
	<batch:job id="fillSerieAndBdFromCsv" job-repository="jobRepository">
		<batch:step id="readWriteFromCsv">
			<batch:tasklet transaction-manager="transactionManage">
				<batch:chunk commit-interval="50"  reader="" writer="" processor=""></batch:chunk>
			</batch:tasklet>
		</batch:step>
	</batch:job>

	<!-- TASKLET -->
	<bean id="helloWolrdTasklet" class="com.cta.tasklet.HelloWolrdTasklet" />

	<!-- READER -->
	<bean id="mesBdReaderCsv" class="org.springframework.batch.item.file.FlatFileItemReader">
	
	</bean>
</beans>
